{% extends 'base.html.twig' %}

{% block title %}{{ 'title.changePassword'|trans }} - {{ parent() }}{% endblock %}

{% block body %}
    <div class="main-header">
        <h2 class="main-title">{{ 'title.changePassword'|trans }}</h2>
    </div>

    <div class="mt-3">
    {{ form(form) }}
    </div>
{% endblock %}

